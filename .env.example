PORT=3000
JWT_SECRET=schimba-cu-un-secret-lung-aleator
NODE_ENV=development
PUBLIC_APP_URL=https://joc-avioanele.vercel.app

# Email (Resend) — required in production for verification + password reset
# https://resend.com — free tier works for testing
# RESEND_API_KEY=re_xxxxxxxx
# EMAIL_FROM=Avioane <onboarding@resend.dev>

# Optional: server-sent SMS (Twilio). Without these, the app opens the phone's SMS app.
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_FROM=+1...
